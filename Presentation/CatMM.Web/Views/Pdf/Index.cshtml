@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <title></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        html, body {
            width: 100%;
            height: 100%;
            margin: 0;
            padding: 0;
        }

        #wraper {
            position: absolute;
            left:0;
            top:0;
            width:100%;
        }
    </style>
</head>
<body>
    <div id="wraper">
        @{
            string googleUrl = "https://maps.googleapis.com/maps/api/staticmap?center=Berkeley,CA&zoom=14&size=400x400&key=AIzaSyBOV4tfWnWm0UBbIvt-bP7IN3vS-4s-mdo";
        }
        <h1>Google map</h1>
        <img src="@googleUrl" />
        <div style="height:50%;overflow:hidden;">
            it has a
            root node called "outline" which contains a number of "item" nodes. An item
            can contain any number of item. These are the outline subsections to the
            section the item represents. A item node has the following attributes:it has a
            root node called "outline" which contains a number of "item" nodes. An item
            can contain any number of item. These are the outline subsections to the
        </div>
        <p id="tip">12</p>
    </div>
    <script src="http://local.catmm.com/assets/plugins/jquery-1.12.0.min.js" type="text/javascript"></script>
    <script>
        $(function () {
            var $ele = $("#wraper");
            var height = $ele.height(),
                width = $ele.width();
            text = "width:" + width + "  height:" + height;
            console.log(text);
            $("#tip").text(text);
        });
    </script>
</body>
</html>
